# Application Settings
APP_NAME=CSI Kalamela FastAPI
DEBUG=False
SECRET_KEY=change-this-secret-key-in-production
ACCESS_TOKEN_EXPIRE_MINUTES=360
ALGORITHM=HS256

# Database
DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/csi_kalamela

# CORS
CORS_ORIGINS=["*"]

# File Upload Settings
MAX_UPLOAD_SIZE_MB=5
ALLOWED_UPLOAD_EXTENSIONS=[".pdf", ".png", ".jpg", ".jpeg", ".webp"]

# Backblaze B2 Storage (Required)
B2_ENDPOINT=https://s3.eu-central-003.backblazeb2.com
B2_BUCKET_NAME=csi-youthmovement
B2_KEY_ID=your_key_id_here
B2_APPLICATION_KEY=your_application_key_here
B2_REGION=eu-central-003

# Email (Optional)
MAIL_SENDER=

# OCR.space — optional PDF payment amount detection (server-side only)
OCR_SPACE_API_KEY=

# Observability — Urgentry (errors) + OTel→Alloy (traces). Leave unset locally.
# ENVIRONMENT=staging
# SENTRY_DSN=https://<key>@bugtracker.infynox.net/<project_id>
# SENTRY_TRACES_SAMPLE_RATE=1.0
# SENTRY_RELEASE=
# OTEL_EXPORTER_OTLP_ENDPOINT=http://10.0.0.181:4317
# OTEL_SERVICE_NAME=csi-api
# OTEL_RESOURCE_ATTRIBUTES=deployment.environment=staging,service.namespace=csi

# Pagination
DEFAULT_PAGE_SIZE=50
